<%= simple_form_for(@land) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.submit class: "btn btn-primary" %>
  <%= f.input :description %>
  t.string "name"
    t.integer "area"
    t.string "flag"
    t.string "description"
    t.integer "population"
    t.bigint "user_id"
<% end %>
